# ============================================================
# 海洋院团总支数字工作台 — 环境变量
# ============================================================

# ---------- PostgreSQL（仅服务端使用） ----------
# 生产环境建议 PostgreSQL 与 Next.js 同机通信，不要暴露 5432 到公网。
# 请将真实值写入 .env 或 .env.local，禁止提交到 Git。
DATABASE_URL=

# 仅用于 npm run db:seed；必须显式设置，项目不会提供默认密码。
SEED_DEFAULT_PASSWORD=

# ---------- 本地文件存储（仅服务端使用） ----------
# 生产示例：/data/tzz/files
# Windows 开发示例：C:/data/ocean-youth/files
FILE_STORAGE_ROOT=

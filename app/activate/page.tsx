'use client';
import { useSearchParams, useRouter } from 'next/navigation'; import { useState } from 'react'; import { useAuth } from '@/hooks/use-auth';
export default function ActivatePage() { const token = useSearchParams().get('token') ?? ''; const router = useRouter(); const { refreshUser } = useAuth(); const [password, setPassword] = useState(''); const [confirmPassword, setConfirm] = useState(''); const [message, setMessage] = useState(''); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setBusy(true); setMessage(''); try { const response = await fetch('/api/auth/activate', { method: 'POST', credentials: 'same-origin', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token, newPassword: password, confirmPassword }) }); const body = await response.json().catch(() => null); if (!response.ok || !body?.success) throw new Error(body?.error?.message ?? '激活失败。'); await refreshUser(); router.replace('/dashboard'); } catch (error) { setMessage(error instanceof Error ? error.message : '激活失败。'); } finally { setBusy(false); } } return <main className="min-h-screen bg-[#FAFAF8] flex items-center justify-center p-5"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border bg-white p-7 shadow-sm space-y-4"><h1 className="text-2xl font-semibold">账号激活</h1><p className="text-sm text-muted-foreground">请设置你的登录密码。密码为 8–128 位，且不能使用管理员初始密码。</p><input required minLength={8} maxLength={128} type="password" placeholder="新密码" value={password} onChange={(e) => setPassword(e.target.value)} className="h-11 w-full rounded-lg border px-3" /><input required minLength={8} maxLength={128} type="password" placeholder="确认新密码" value={confirmPassword} onChange={(e) => setConfirm(e.target.value)} className="h-11 w-full rounded-lg border px-3" />{message && <p className="text-sm text-destructive">{message}</p>}<button disabled={busy || !token} className="h-11 w-full rounded-lg bg-foreground text-background disabled:opacity-50">{busy ? '激活中…' : '设置密码并激活'}</button></form></main>; }

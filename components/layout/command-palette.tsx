'use client';
import { useEffect, useCallback } from 'react'; import { useRouter } from 'next/navigation'; import { CommandDialog, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList } from '@/components/ui/command'; import { LayoutDashboard, CheckSquare, ListTodo, FolderOpen, ClipboardCheck, Calendar, FileStack, Building2, Users } from 'lucide-react';
const pages = [['/dashboard','工作台',LayoutDashboard],['/tasks/my','我的任务',CheckSquare],['/tasks','任务管理',ListTodo],['/files','材料中心',FolderOpen],['/reviews','审核中心',ClipboardCheck],['/calendar','工作日历',Calendar],['/templates','模板中心',FileStack],['/departments','部门',Building2],['/members','成员',Users]] as const;
export function CommandPalette({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) { const router = useRouter(); useEffect(() => { const h=(e: KeyboardEvent)=>{ if(e.key==='k'&&(e.metaKey||e.ctrlKey)){e.preventDefault();onOpenChange(!open);} }; document.addEventListener('keydown',h); return()=>document.removeEventListener('keydown',h); },[open,onOpenChange]); const go=useCallback((p:string)=>{onOpenChange(false);router.push(p);},[router,onOpenChange]); return <CommandDialog open={open} onOpenChange={onOpenChange}><CommandInput placeholder="搜索页面…"/><CommandList><CommandEmpty>没有找到相关页面</CommandEmpty><CommandGroup heading="页面">{pages.map(([path,label,Icon])=><CommandItem key={path} onSelect={()=>go(path)}><Icon className="mr-2 h-4 w-4 text-muted-foreground"/>{label}</CommandItem>)}</CommandGroup></CommandList></CommandDialog>; }
